# Verdict pass: movement editing recovery

Disposition: **ship**, limited to the two material fixes in [finish-review.md](finish-review.md).

Both findings are **resolved**. This pass verifies that fix list; it is not a new whole-surface audit, a replacement for the initial review, or a production deployment approval. The initial review is preserved.

| Material fix | Score | Verification |
|---|---|---|
| Preserve an already-open editor through wallet archive and management permission changes | **Resolved** | The edit route records the opened transaction and retains its form through subsequent archive/permission updates. Initial-entry restrictions remain. `TransactionEdit` includes live management access and archive state in its disabled condition and shows an in-place explanation. The new mobile/desktop captures retain the entered `19000`, description, and local `adicional.txt`; review is disabled and cancellation remains available. Scenario 12 exercises live archive/restoration and permission revocation/restoration, checks retained work, verifies re-enabled review, and confirms the original amount remains `18500` before save with no edit drafts. |
| Recover unavailable upload batches and make local discard independent of cleanup | **Resolved** | `useTransactionEdit` recognizes `UPLOAD_EXPIRED` and `UPLOAD_NOT_FOUND`, clears the unusable attempt, and allows the next explicit save to upload the same retained files. Unknown-outcome errors retain the attempt for idempotent confirmation. `cancel()` starts best-effort cleanup without awaiting it, clears the attempt, and permits local discard immediately. Pre-finalization failure cleanup also releases the operation without awaiting reconnection. Tests cover direct and wrapped error codes and leave transport failures unclassified. Scenario 12 verifies same-file retry after an actual batch deletion and an injected expiry response; replaces an actual successful finalize response with failure and verifies an identical retry creates no duplicate movement/files; then disconnects Convex after an interrupted finalize and verifies cancellation reaches the detail URL within five seconds without changing registered amount/files or creating drafts. |

The reviewer inspected the relevant route, edit component, local controller, error-code helper/tests, tracked scenario 12, its recorded result, and the updated validation/attachment/browser reports. The reviewer opened the four new availability captures and the six recaptured edit, edit-confirm, and cancel captures at 390 × 844 and 1440 × 1100. The new notices fit the incumbent composition, explain how to recover, and preserve access to the pending values and cancellation. The normal edit and cancel presentation remains consistent with the initial review. No browser or detector was run by this reviewer; the original empty detector report remains the detector evidence.

The supplied validation records passing lint, TypeScript, whitespace checks, production build, and **111 tests in 18 files**. One build needed a retry after a Google Fonts download failure; the unchanged retry passed. The attachment report aggregates **12 passing scenarios**, with recovery evidence in `output/transaction-files-qa/2026-09-14T03-02-42/results.json`. The updated normal browser report continues to record no mutations before edit confirmation and no draft mutations during normal edit/cancel/save.

The recovery test keeps the Next application reachable while disconnecting Convex WebSockets. It establishes that local cancellation no longer waits for Convex cleanup. It does **not** establish full offline browsing: a fully offline trial reached a browser network error during navigation, and that limitation is explicitly recorded. Expiry recovery uses fault injection rather than waiting 24 hours. Runtime tests were supplied by the implementation pass and inspected here, not rerun independently.

No unresolved or partial item remains from this fix list. Proceed with the documentation handoff, retaining the stated local-edit and offline-navigation limits. The external AI provider, shared-server standard main-flow Playwright suite, and production deployment remain outside the verified scope.
