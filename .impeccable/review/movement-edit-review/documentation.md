# Documentation handoff

Documentation complete for the approved B flow and the later requirement that normal editing creates no drafts. Updated `README.md`, `PRODUCT.md`, `docs/movement-flow.md`, and this review's `contract.md`. App code, `docs/ux-review/`, and `.impeccable/review/movement-flow/` were preserved in this handoff.

The documents now distinguish creation drafts (24-hour lifetime, 650 ms autosave), local normal editing (Completar → Confirmar, explicit Guardar cambios), and recovery of preexisting edit drafts through explicit links. They record independent attachment/AI permissions, local file handling, revision/currency/file checks after uploads, idempotent recovery, preserved work through live archive/permission changes, and nonblocking best-effort cancellation cleanup. They also distinguish currency recovery for creation (new draft) and editing (reopen the movement), and require Convex schema/functions/index deployment before the frontend.

Read `reference/document.md` and compared the finished surface with the incumbent source in `app/globals.css`, `app/movements.css`, and `app/layout.tsx`. Checked cream and light surface tokens; teal primary/hover/soft; coral expense and blue income roles; foreground, border, warning and shadow tokens; Geist Sans; inherited buttons/fields; and scoped focus, tabular amounts, 650 px column, 520 px responsive adjustment, 44 px targets and 48 px primary actions. Opened `permission-edit-mobile.png` and `edit-confirm-desktop.png` to confirm the retained notice, form, attachments and confirmation use that vocabulary. These were source/capture comparisons, not a new computed-contrast or keyboard audit. The implementation extends the incumbent system: no new visual world was approved, and no `DESIGN.md` or `.impeccable/design.json` was fabricated or replaced.

Current evidence lives in this folder: **111 tests in 18 files**, lint, typecheck, whitespace and production build passed; one unchanged build retry followed a font download failure. Real Clerk/local Convex/MinIO checks on `localhost:3034` record no mutations before normal edit confirmation and no draft mutations throughout normal edit/cancel/save. There are **18 captures** at mobile/desktop sizes and **12 passing attachment scenarios**. The reports retain the authentication retry, the corrected balance assertion, and scenario 12's explicit missing/expired-batch, lost-response and Convex-disconnection fault injection. Next remained reachable for the cancellation check; full offline navigation is unsupported, and browser history can abandon local edits. No persistence after leaving is promised.

The initial `finish-review.md` remains **fix**. `verdict.md` marks its two material recovery issues resolved with **ship limited to that fix list**, not a new whole-surface or production approval. Its supplied runtime results were inspected rather than rerun by the documenter; the original zero-finding detector was not rerun. The external AI provider and standard main-flow Playwright suite against shared `localhost:3000` were not exercised. No PR exists for this branch; the review concerns local changes, without commit or production deployment.
